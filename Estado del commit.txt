This commits install the flask-WTF librarie to use WTForms. 
It also create a forms.py file that creates the class AddTaskForm, so a form can be created with an structure of Stringfield and submitfield.

It creates a new route that contains the new form.