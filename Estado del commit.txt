This commits uses the jinja function to extends a template, homepage and goodbye page copy the base.html content except the block main
the block main is individually edited on each page to set the required message.