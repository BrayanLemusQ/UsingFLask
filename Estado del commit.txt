This commits install the flask-SQLAlchemy librarie

It validates the submit bottom, when this bottom is activated the value on the StringField is captrated and post it as a subtitle h1 in the add page
The routes.py is modified so the add function validates the activation of the submitfield
The add.html file is modified to change by the time a new value for title appears.