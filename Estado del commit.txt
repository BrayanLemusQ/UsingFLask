This commits changes the templates:
creates:
    delete.html
        a delete.html is created to show a question that ask if the task should be deleted or not

changes: 
    routes.py
        A delete route is created, this function allows to delete a specific task. It receive the task_id as a parameter and validates if the submmit bottom is activated, if is not the task remains the same.
        It also show flash messages that announce if a change was made or not. 
    index.html
        the url is modified so the delete bottom leads to delete.html 
    form.py
        a new class is created to show a new form that shows the task a delet bottom and a cancel bottom
